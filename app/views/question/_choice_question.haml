.choice_question
  %p
    %span= q.content.html_safe
  %p
  - q.items.each_with_index do |e, index|
    %span= (d2c(index) + e.render_tex).html_safe
    - if index % q.item_num_per_row != 0
      %br
